#!/usr/bin/env sh

# Placeholder gradlew script.
# Use the official Fabric template generator to get a full Gradle wrapper,
# or run: gradle wrapper --gradle-version 8.12

echo "This zip is a source template. Generate a Gradle wrapper before building."
exit 1
